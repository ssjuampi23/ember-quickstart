<!-- The @ indicates that @title is an argument passed to this component -->
<h2>{{@title}}</h2>

<ul>
  {{#each @people as |person|}}
    <li>
      <button {{on 'click' (fn this.showPerson person)}} type="button">{{person}}</button>
    </li>
  {{/each}}
</ul>